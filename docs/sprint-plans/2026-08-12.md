# Sprint Plan — Week of August 12

Program timeline: Demo Day is **September 23, 2026** at Google HQ — 6 weeks
out from this sprint. See @docs/PROJECT_CONTEXT.md for full program context
and @CLAUDE.md for current build status.

## Carried over from last week (unresolved, not new)

These were flagged as blockers or open items in prior status reports and
PROJECT_CONTEXT.md's Action Items — nothing below is newly discovered this
week, just still outstanding.

- [ ] **`ANTHROPIC_API_KEY` still not set on Railway** — blocks *all*
      generation on the live site (including this week's new 3-variant
      picker). Blocked on Maria's budget sign-off, not code. Everything
      generation-related is tested and working locally with a personal key.
- [ ] **Meta App Review not yet submitted** — flagged Aug 8 as the pacing
      item against Demo Day (Meta's own stated review window is 6–8 weeks;
      we're now at ~6 weeks to Sept 23). This is the single most
      time-sensitive item on this list — starting it late risks Demo Day.
- [ ] **ESP (email service provider) selection unconfirmed with Felix** —
      needed before the eblast-sending feature can be built for real, and
      before Vertical Response's 10,000/month credit limit becomes a
      problem at the 50–60k/month volume Maria mentioned.
- [ ] **Confirm "Key Makers" definition** with Nancy/Felix — Nancy's 10 real
      testimonial clients (seeded) vs. Felix's 4 GHL personas are still two
      unreconciled concepts; matters before audience fields get tagged
      against either one.
- [ ] **Confirm full-newsletter-vs-individual-blocks** generation model —
      open question blocking a scope decision on newsletter UX.

## This week's priorities

### 1. Verify the live deploy end-to-end
The frontend went live on Vercel this week and CORS/build issues are
resolved, but the full loop (event form → generate → review → calendar)
hasn't been confirmed working against the Railway backend with a real
key. Do this as soon as `ANTHROPIC_API_KEY` lands, or with a temporary
personal key if sign-off is still pending and a demo is needed sooner.

- [ ] Confirm `NEXT_PUBLIC_API_URL` and `CORS_ALLOWED_ORIGINS` are correct
      for the current Vercel URL
- [ ] Walk through the new 3-variant social post/hashtag picker on the live
      site once a key is available — this was only tested locally
- [ ] Spot-check the calendar page and Keymakers toggle on live data

### 2. Start Meta App Review (highest priority, longest lead time)
Given the 6–8 week window, this needs to start now regardless of what else
is in flight — it can run in parallel with everything below.

- [ ] Convert WVF's Instagram to a Professional (Business/Creator) account
- [ ] Begin Meta business verification
- [ ] Draft the App Review submission, including the required screencast
      for `instagram_content_publish`

### 3. Newsletter blocks — surface in the frontend
`newsletter_blocks` (feature article, events list, grant/flyer, tips/CTA,
member spotlight, boilerplate) has backend schemas, generators, and a
router built (per CLAUDE.md Status) but no frontend UI yet to actually
generate or review them. This is the largest built-but-not-exposed gap in
the app right now.

- [ ] Scope and build a frontend flow for generating/reviewing individual
      newsletter blocks (pending the block-vs-full-newsletter decision above
      — may need Nancy/Felix's answer first, or can proceed with the
      individual-blocks path since that's already the built backend shape)

### 4. Follow up on open client-side decisions
Not code work, but blocking code work if left unanswered much longer.

- [ ] Send a follow-up to Nancy/Felix on the Key Makers definition and
      full-newsletter-vs-blocks questions — both have been open for over a
      week
- [ ] Follow up with Felix on ESP selection

## Explicitly not this week

Per CLAUDE.md's Out of Scope list — direct social publishing, post
scheduling automation, analytics, comment/DM management, and a background
job queue remain out of scope unless the client formally reopens that
conversation. The Meta App Review work above is prerequisite groundwork
for a future manual-click publish feature (confirmed in scope Aug 8), not
a scope change on its own.

## Reference

- Full project context, confirmed scope, and program requirements:
  @docs/PROJECT_CONTEXT.md
- What's actually generated today vs. known gaps: @docs/CONTENT_SCOPE.md
- Current build status: @CLAUDE.md
